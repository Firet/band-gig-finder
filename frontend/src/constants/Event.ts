import Event from '@/interfaces/Event'

const GigEvents: Event[] = [
    {
        title: 'Gigafest',
        body: '',
        user_id: 1,
        status: '',
        created_at: new Date(),
        event_date: new Date(),
    }
];

export default GigEvents;